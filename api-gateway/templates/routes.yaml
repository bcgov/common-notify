kind: GatewayService
name: ${GATEWAY_SERVICE_NAME}
tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
host: ${BACKEND_HOST}
port: 80
protocol: http
retries: 0
routes:
  # ============================================================================
  # PUBLIC ROUTES - No authentication required
  # ============================================================================
  - name: ${ROUTE_PREFIX}notify-health
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/health
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-api-health
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/health
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # ============================================================================
  # SERVICE-TO-SERVICE ROUTES (Client Credentials)
  # Routes that can only be called by backend services using service tokens
  # ============================================================================
  - name: ${ROUTE_PREFIX}ches-email
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/ches/api/v1/email
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-simple
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/notifysimple
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-simple-cancel-reschedule
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/notifysimple/[^/]+
    methods:
      - PATCH
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-simple-frontend-cancel-reschedule
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/notifysimple/[^/]+
    methods:
      - PATCH
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-event
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/notifyevent
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-event-preview
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/notifyevent/preview
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-event-types
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/notifyevent/types
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-list
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/notify
    methods:
      - GET
      - DELETE
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-status
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/notify/status
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}notify-callbacks
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/notify/registerCallback
    methods:
      - POST
      - PATCH
      - DELETE
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}templates
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/templates
    methods:
      - GET
      - POST
      - PATCH
      - DELETE
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}feature-flags
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/feature-flags
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}feature-flags-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/feature-flags
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  # GC Notify-compatible routes: sub-paths/shapes mirror the real GC Notify v2 API
  # exactly (https://documentation.notification.canada.ca/en/apispec.html). Kept
  # under a /gcnotify prefix (no /api) so this traffic stays unambiguous in logs,
  # metrics, and Kong dashboards - migrating GC Notify integrations only need to
  # set their baseUrl to {hostname}/gcnotify, a one-line config change, not a code
  # change.
  - name: ${ROUTE_PREFIX}gcnotify-notifications-list
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify/v2/notifications
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-notifications-email
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify/v2/notifications/email
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-notifications-sms
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify/v2/notifications/sms
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-notifications-bulk
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify/v2/notifications/bulk
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-notification-by-id
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      # Regex, not a literal prefix - matches a single path segment so it doesn't
      # shadow the literal /gcnotify/v2/notifications/email|sms|bulk routes above
      # (Kong matches literal paths before regex paths).
      - ~${PATH_PREFIX}/gcnotify/v2/notifications/[^/]+
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-templates-list
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify/v2/templates
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-template-by-id
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/gcnotify/v2/template/[^/]+
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  # gcnotify-passthrough/v2 routes: same GC Notify-compatible operations as gcnotify/v2
  # but served to clients that have not yet been onboarded to the Notify tenant system.
  # ApiKeyGuard (format-only) validates Authorization header shape in backend and forwards
  # the original header directly to GC Notify. No Kong key-auth on these routes because
  # passthrough uses GC Notify-managed API keys, not Kong-managed credentials.
  - name: ${ROUTE_PREFIX}gcnotify-passthrough-notifications-list
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify-passthrough/v2/notifications
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-passthrough-notifications-email
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify-passthrough/v2/notifications/email
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-passthrough-notifications-sms
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify-passthrough/v2/notifications/sms
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-passthrough-notifications-bulk
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify-passthrough/v2/notifications/bulk
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-passthrough-notification-by-id
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/gcnotify-passthrough/v2/notifications/[^/]+
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-passthrough-templates-list
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/gcnotify-passthrough/v2/templates
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  - name: ${ROUTE_PREFIX}gcnotify-passthrough-template-by-id
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/gcnotify-passthrough/v2/template/[^/]+
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # ============================================================================
  # SERVICE & FRONTEND ROUTES
  # Routes that support BOTH service tokens AND frontend user tokens
  # Note: Each route appears twice (-service and -frontend variants)
  #       to handle different issuer/audience combinations
  # ============================================================================

  # /api/v1/code-tables (service variant)
  - name: ${ROUTE_PREFIX}code-tables-service
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/code-tables
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  # /api/v1/code-tables (frontend variant)
  - name: ${ROUTE_PREFIX}code-tables-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/code-tables
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/code-tables/notification-status (service variant)
  - name: ${ROUTE_PREFIX}code-tables-status-service
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/code-tables/notification-status
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  # /api/v1/code-tables/notification-status (frontend variant)
  - name: ${ROUTE_PREFIX}code-tables-status-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/code-tables/notification-status
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/code-tables/channels (service variant)
  - name: ${ROUTE_PREFIX}code-tables-channels-service
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/code-tables/channels
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  # /api/v1/code-tables/channels (frontend variant)
  - name: ${ROUTE_PREFIX}code-tables-channels-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/code-tables/channels
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/code-tables/event-types (service variant)
  - name: ${ROUTE_PREFIX}code-tables-event-types-service
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/code-tables/event-types
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  # /api/v1/code-tables/event-types (frontend variant)
  - name: ${ROUTE_PREFIX}code-tables-event-types-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/code-tables/event-types
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/code-tables/feature-flags (service variant)
  - name: ${ROUTE_PREFIX}code-tables-feature-flags-service
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/code-tables/feature-flags
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/code-tables/feature-flags (frontend variant)
  - name: ${ROUTE_PREFIX}code-tables-feature-flags-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/code-tables/feature-flags
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/notification_request (service variant)
  - name: ${ROUTE_PREFIX}notification-request-service
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/notification_request
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  # /api/v1/notification_request (frontend variant)
  - name: ${ROUTE_PREFIX}notification-request-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/notification_request
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true
  # /api/v1/frontend/notification_request/events (frontend only)
  - name: ${ROUTE_PREFIX}notification-request-events-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/notification_request/events
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: false
    response_buffering: false

  # /api/v1/frontend/admin/tenants (frontend admin only - get all tenants)
  - name: ${ROUTE_PREFIX}admin-tenants-list-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/admin/tenants
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/users/me (upsert current user)
  - name: ${ROUTE_PREFIX}admin-users-upsert-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/users/me
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/users (get all users)
  - name: ${ROUTE_PREFIX}admin-users-list-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/users
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # ============================================================================
  # AUTH ROUTES (authenticated users)
  # ============================================================================
  # /api/v1/frontend/auth/tenants (get user's CSTAR tenants)
  - name: ${ROUTE_PREFIX}auth-tenants-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/auth/tenants
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/auth/tenants/:tenantId/roles (get user's roles in tenant)
  - name: ${ROUTE_PREFIX}auth-tenant-roles-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/auth/tenants/[^/]+/roles
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # ============================================================================
  # FEATURE FLAG ADMIN ROUTES (admin only)
  # ============================================================================
  # /api/v1/frontend/admin/feature-flags (list all)
  - name: ${ROUTE_PREFIX}admin-feature-flags-list-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/admin/feature-flags
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/admin/feature-flags (create)
  - name: ${ROUTE_PREFIX}admin-feature-flags-create-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/admin/feature-flags
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/admin/feature-flags/:id (update)
  - name: ${ROUTE_PREFIX}admin-feature-flags-update-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/admin/feature-flags/
    methods:
      - PATCH
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/admin/feature-flags/:id (delete)
  - name: ${ROUTE_PREFIX}admin-feature-flags-delete-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/admin/feature-flags/
    methods:
      - DELETE
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/templates (frontend variant - list and create)
  - name: ${ROUTE_PREFIX}templates-frontend-list
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/templates
    methods:
      - GET
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/templates POST (frontend variant - create)
  - name: ${ROUTE_PREFIX}templates-frontend-create
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/templates
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/templates/:id (frontend variant - get by id, update, delete)
  - name: ${ROUTE_PREFIX}templates-frontend-by-id
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/templates/*
    methods:
      - GET
      - POST
      - DELETE
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v1
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/templates/:id/preview (frontend variant - preview templates)
  - name: ${ROUTE_PREFIX}templates-frontend-preview
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/templates/*/preview
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v1
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/templates/preview (frontend variant - preview arbitrary body)
  - name: ${ROUTE_PREFIX}templates-frontend-preview-body
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/templates/preview
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/events (frontend variant - list and create)
  # Frontend-only: events are configured by tenant users through the UI, so there
  # is no service-token variant.
  - name: ${ROUTE_PREFIX}events-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/events
    methods:
      - GET
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/events/:eventId (frontend variant - get by id, update)
  # Regex matching a single path segment, same convention as the auth tenant
  # roles route. Update is a POST, matching the templates controller.
  - name: ${ROUTE_PREFIX}events-frontend-by-id
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/events/[^/]+
    methods:
      - GET
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/events/:eventId/channels/email (frontend variant - email tab settings)
  # Declared explicitly rather than relying on the by-id route's prefix match, same
  # convention as the auth tenant roles route.
  - name: ${ROUTE_PREFIX}events-frontend-email-channel
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/events/[^/]+/channels/email
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/events/:eventId/channels/email/draft (frontend variant - save email tab as draft)
  # Declared explicitly, same convention as events-frontend-email-channel.
  - name: ${ROUTE_PREFIX}events-frontend-email-channel-draft
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/events/[^/]+/channels/email/draft
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/events/:eventId/channels/email/active (frontend variant - toggle email channel active)
  - name: ${ROUTE_PREFIX}events-frontend-email-channel-active
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/events/[^/]+/channels/email/active
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/events/:eventId/channels/sms (frontend variant - sms tab settings)
  - name: ${ROUTE_PREFIX}events-frontend-sms-channel
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/events/[^/]+/channels/sms
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/events/:eventId/channels/sms/draft (frontend variant - save sms tab as draft)
  - name: ${ROUTE_PREFIX}events-frontend-sms-channel-draft
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/events/[^/]+/channels/sms/draft
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/events/:eventId/channels/sms/active (frontend variant - toggle sms channel active)
  - name: ${ROUTE_PREFIX}events-frontend-sms-channel-active
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/events/[^/]+/channels/sms/active
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # ============================================================================
  # API KEY MANAGEMENT ROUTES
  # ============================================================================
  # /api/v1/service/api-key/bind
  # Binds an API key to a CSTAR tenant.
  # Requires: X-API-KEY header (validated by Kong) AND Authorization: Bearer <jwt>
  # Kong validates the API key and forwards x-credential-identifier to the backend.
  # The backend uses the JWT to verify CSTAR tenant membership before storing the mapping.
  - name: ${ROUTE_PREFIX}api-key-bind
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/service/api-key/bind
    methods:
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/api-key-usage (frontend variant - usage, history, threshold)
  # Called by the Notify web app with a frontend user JWT (never a service API key).
  # Kong prefix-matches this base path, so it also covers /history (GET) and
  # /threshold (PATCH). The backend enforces exact method+path and CSTAR roles.
  #
  # NOTE: This must be declared BEFORE the /api/v1/frontend/admin/api-key-usage route
  # is NOT a concern here because Kong matches the longest/most-specific prefix, and
  # the admin route lives under a different path segment (/frontend/admin/...).
  - name: ${ROUTE_PREFIX}api-key-usage-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/api-key-usage
    methods:
      - GET
      - PATCH
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/tenant-settings (frontend variant)
  # Called by the Notify web app with a frontend user JWT (never a service API key).
  - name: ${ROUTE_PREFIX}tenant-settings-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/tenant-settings
    methods:
      - GET
      - PATCH
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/safelist (frontend variant)
  # Recipient safelist management from the Settings page. Called by the Notify web app with a
  # frontend user JWT (never a service API key) - the safelist is administered by tenant admins,
  # not by integrating services.
  - name: ${ROUTE_PREFIX}safelist-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/safelist
    methods:
      - GET
      - POST
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/safelist/{id} (frontend variant)
  # Separate route because DELETE is not a method on the collection path above; same reason
  # the notifysimple cancel/reschedule PATCH gets its own regex route.
  - name: ${ROUTE_PREFIX}safelist-entry-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ~${PATH_PREFIX}/api/v1/frontend/safelist/[^/]+
    methods:
      - DELETE
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

  # /api/v1/frontend/admin/api-key-usage (admin variant - all tenants' usage)
  # Admin-only (NOTIFY_ADMIN SSO role), not tenant-scoped. Same jwt-keycloak plugin
  # as the other frontend admin routes. Kong prefix-matches, so PATCH also covers
  # /api/v1/frontend/admin/api-key-usage/limits.
  - name: ${ROUTE_PREFIX}admin-api-key-usage-frontend
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    hosts:
      - ${GATEWAY_HOSTNAME}
    paths:
      - ${PATH_PREFIX}/api/v1/frontend/admin/api-key-usage
    methods:
      - GET
      - PATCH
    strip_path: true
    https_redirect_status_code: 426
    path_handling: v0
    request_buffering: true
    response_buffering: true

plugins:
  # ============================================================================
  # SERVICE-TO-SERVICE PLUGINS
  # ============================================================================
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}api-key-bind
    config:
      hide_credentials: false
      key_in_body: false
      key_in_header: true
      key_names: ["X-API-KEY"]
      run_on_preflight: true

  # Rate limiting (per API key) - applies only to key-auth (service-to-service) callers.
  # policy: redis uses a centralized counter shared across all Kong pods, so the limit is
  # enforced cluster-wide per key regardless of replica count (adds ~100-200ms latency).
  # fault_tolerant: true fails open if Redis is unreachable - a Redis outage degrades to no
  # limiting rather than blocking traffic. Redis is provided by the managed BC Gov gateway.
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}api-key-bind
    config: &rateLimitConfig
      limit_by: credential
      policy: redis
      fault_tolerant: true
      hide_client_headers: false
      minute: ${API_KEY_RATE_LIMIT_PER_MINUTE}
      second: null
      hour: null
      day: null
      month: null
      year: null
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}ches-email
    config: &keyAuthConfig
      hide_credentials: true
      key_in_body: false
      key_in_header: true
      key_names: ["X-API-KEY"]
      run_on_preflight: true

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}ches-email
    config: *rateLimitConfig
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notify-simple
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-simple
    config: *rateLimitConfig
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notify-simple-cancel-reschedule
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-simple-cancel-reschedule
    config: *rateLimitConfig
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-simple-frontend-cancel-reschedule
    config: &jwtConfig
      allowed_iss:
        - ${FRONTEND_KEYCLOAK_ISSUER}
      allowed_aud: ${FRONTEND_ALLOWED_AUDIENCE}
      run_on_preflight: true
      iss_key_grace_period: 10
      maximum_expiration: 0
      algorithm: RS256
      claims_to_verify:
        - exp
      uri_param_names:
        - jwt
      cookie_names: []
      scope: null
      roles: null
      realm_roles: null
      client_roles: null
      anonymous: null
      consumer_match: true
      consumer_match_claim: azp
      consumer_match_claim_custom_id: true
      consumer_match_ignore_not_found: true
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notify-event
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-event
    config: *rateLimitConfig
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notify-event-preview
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-event-preview
    config: *rateLimitConfig
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notify-event-types
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-event-types
    config: *rateLimitConfig
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notify-list
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-list
    config: *rateLimitConfig
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notify-status
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-status
    config: *rateLimitConfig
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notify-callbacks
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notify-callbacks
    config: *rateLimitConfig
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}templates
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}templates
    config: *rateLimitConfig
  # GC Notify clients authenticate with a single credential, sent the same way real
  # GC Notify expects it: `Authorization: ApiKey-v1 {key}`. They aren't expected to
  # also send a separate X-API-KEY header. pre-function extracts the key from that
  # header into X-API-KEY (which key-auth below is configured to validate, same as
  # every other route in this file) before key-auth's access phase runs - pre-function
  # plugins run first by Kong's plugin priority ordering. The original Authorization
  # header is left untouched so passthrough mode can still forward it to real GC
  # Notify. NOT YET VALIDATED against a live Kong instance - confirm this works
  # against the BC Gov-managed gateway (plugin availability/ordering) before merging.
  - name: pre-function
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-list
    config: &gcnotifyAuthExtract
      access:
        - |
          local auth = kong.request.get_header("authorization")
          if auth then
            local key = auth:match("^%s*ApiKey%-v1%s+(.+)$")
            if key then
              ngx.req.set_header("X-API-KEY", key)
            end
          end
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-list
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-list
    config: *rateLimitConfig
  - name: pre-function
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-email
    config: *gcnotifyAuthExtract
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-email
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-email
    config: *rateLimitConfig
  - name: pre-function
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-sms
    config: *gcnotifyAuthExtract
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-sms
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-sms
    config: *rateLimitConfig
  - name: pre-function
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-bulk
    config: *gcnotifyAuthExtract
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-bulk
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notifications-bulk
    config: *rateLimitConfig
  - name: pre-function
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notification-by-id
    config: *gcnotifyAuthExtract
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notification-by-id
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-notification-by-id
    config: *rateLimitConfig
  - name: pre-function
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-templates-list
    config: *gcnotifyAuthExtract
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-templates-list
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-templates-list
    config: *rateLimitConfig
  - name: pre-function
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-template-by-id
    config: *gcnotifyAuthExtract
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-template-by-id
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}gcnotify-template-by-id
    config: *rateLimitConfig
  # ============================================================================
  # SERVICE & FRONTEND PLUGINS
  # Each route appears twice: once for service tokens, once for frontend tokens
  # ============================================================================

  # Code Tables - Service Token
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-service
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-service
    config: *rateLimitConfig

  # Code Tables - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-frontend
    config: *jwtConfig

  # Code Tables Status - Service Token
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-status-service
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-status-service
    config: *rateLimitConfig

  # Code Tables Status - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-status-frontend
    config: *jwtConfig

  # Code Tables Channels - Service Token
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-channels-service
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-channels-service
    config: *rateLimitConfig

  # Code Tables Channels - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-channels-frontend
    config: *jwtConfig

  # Code Tables Event Types - Service Token
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-event-types-service
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-event-types-service
    config: *rateLimitConfig

  # Code Tables Event Types - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-event-types-frontend
    config: *jwtConfig

  # Notification Request - Service Token
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}notification-request-service
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notification-request-service
    config: *rateLimitConfig

  # Notification Request - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notification-request-frontend
    config: *jwtConfig

  # Notification Request Events - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}notification-request-events-frontend
    config: *jwtConfig

  # Admin Tenants List - Frontend Admin Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}admin-tenants-list-frontend
    config: *jwtConfig

  # Admin Users Upsert (me) - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}admin-users-upsert-frontend
    config: *jwtConfig

  # Admin Users List - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}admin-users-list-frontend
    config: *jwtConfig

  # ============================================================================
  # FEATURE FLAG ADMIN ROUTES PLUGINS
  # ============================================================================
  # Admin Feature Flags List - Frontend Admin Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}admin-feature-flags-list-frontend
    config: *jwtConfig

  # Admin Feature Flags Create - Frontend Admin Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}admin-feature-flags-create-frontend
    config: *jwtConfig

  # Admin Feature Flags Update - Frontend Admin Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}admin-feature-flags-update-frontend
    config: *jwtConfig

  # Admin Feature Flags Delete - Frontend Admin Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}admin-feature-flags-delete-frontend
    config: *jwtConfig

  # ============================================================================
  # FEATURE FLAG CLIENT & CODE-TABLES PLUGINS
  # ============================================================================
  # Feature Flags - Service Token
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}feature-flags
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}feature-flags
    config: *rateLimitConfig

  # Feature Flags Frontend - Frontend Token (authenticated users)
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}feature-flags-frontend
    config: *jwtConfig

  # Auth Tenants - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}auth-tenants-frontend
    config: *jwtConfig

  # Auth Tenant Roles - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}auth-tenant-roles-frontend
    config: *jwtConfig

  # Code Tables Feature Flags - Service Token
  - name: key-auth
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    protocols: [http, https]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-feature-flags-service
    config: *keyAuthConfig

  # Rate limiting (per API key) - see &rateLimitConfig for details
  - name: rate-limiting
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-feature-flags-service
    config: *rateLimitConfig

  # Code Tables Feature Flags - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}code-tables-feature-flags-frontend
    config: *jwtConfig

  # Templates Frontend - List and Create
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}templates-frontend-list
    config: *jwtConfig

  # Templates Frontend - Create
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}templates-frontend-create
    config: *jwtConfig

  # Templates Frontend - Get by ID, Update, Delete
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}templates-frontend-by-id
    config: *jwtConfig

  # Templates Frontend - Preview
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}templates-frontend-preview
    config: *jwtConfig

  # Templates Frontend - Preview Body
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}templates-frontend-preview-body
    config: *jwtConfig

  # Events Frontend - List and Create
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}events-frontend
    config: *jwtConfig

  # Events Frontend - Get by ID, Update
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}events-frontend-by-id
    config: *jwtConfig

  # Events Frontend - Email Channel Settings
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}events-frontend-email-channel
    config: *jwtConfig

  # Events Frontend - Email Channel Draft
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}events-frontend-email-channel-draft
    config: *jwtConfig

  # Events Frontend - Email Channel Active
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}events-frontend-email-channel-active
    config: *jwtConfig

  # Events Frontend - SMS Channel Settings
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}events-frontend-sms-channel
    config: *jwtConfig

  # Events Frontend - SMS Channel Draft
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}events-frontend-sms-channel-draft
    config: *jwtConfig

  # Events Frontend - SMS Channel Active
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}events-frontend-sms-channel-active
    config: *jwtConfig

  # API Key Usage Frontend - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}api-key-usage-frontend
    config: *jwtConfig

  # Tenant Settings Frontend - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}tenant-settings-frontend
    config: *jwtConfig

  # Admin API Key Usage Frontend - Frontend Admin Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}admin-api-key-usage-frontend
    config: *jwtConfig

  # Safelist Frontend - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}safelist-frontend
    config: *jwtConfig

  # Safelist Entry Frontend - Frontend Token
  - name: jwt-keycloak
    tags: [ns.${GATEWAY_ID}.${ENVIRONMENT}]
    enabled: true
    route: ${ROUTE_PREFIX}safelist-entry-frontend
    config: *jwtConfig

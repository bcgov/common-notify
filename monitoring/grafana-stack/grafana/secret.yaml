apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-credentials
  namespace: f6bc3f-dev
type: Opaque
stringData:
  # Admin username
  admin-user: admin
  # Admin password - CHANGE THIS AFTER FIRST DEPLOYMENT
  # Generate a strong password with: openssl rand -base64 32
  admin-password: changeme123!

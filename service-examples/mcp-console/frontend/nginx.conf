server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  location /api {
    # Point at the mcp-console backend when containerizing both together.
    proxy_pass http://backend:3100;
  }

  location / {
    try_files $uri /index.html;
  }
}
